// import React from "react";
// import { Outlet, Navigate } from "react-router-dom";

// const ProtectRoutes = () => {
//   // Retrieve signed-in status from localStorage
//   const auth = localStorage.getItem("signed_in");

//   // Check if auth is set and is "true"
//   if (auth && auth === "true") {
//     return <Outlet />;
//   } else {
//     // If not signed in, redirect to signup page
//     return <Navigate to="/signup" />;
//   }
// };

// export default ProtectRoutes;
